### 22200066 김동하 05분반

##### 프로그래밍 스튜디오 DS Lab 001

### 소감

---


+ **1번문제** bmi 계산하는 문제다. 
>항상 풀어봤던 문제고, 쉽다. 
>어려운 부분은 깃을 연결하는 부분이다.


+ **2번문제** 좌표간 거리 계산이다. 
>그냥 새로운거 include 해서 사용하면 된다. 
>cmath 오랜만에 써서 반갑다


+ **3번문제** 날짜와 달 입력하면 몇번째 날인지 알려주는 문제다. 
>if문 쓰면 귀찮아지니 switch case에 break를 안 거는 식으로 했다.
>case도 오랜만에 써서 반갑다.


+ **4번문제** 세 가지 과목의 점수를 입력받아서 평균과 Pass Fail 여부를 판단하는 건데, 가장 어려웠던 점은 소숫점을 나타내는 것이다.
>검색해서 cout.precision() 함수를 사용해서 풀었다.


+ **5번문제** 이거 생각보다 생각할 것이 많아서 놀랐다.
> 일단 <ins>getline(cin, word);</ins> 을 사용해서 공백을 포함한 **String** 으로받았고, 문제에 필요한대로 아스키 코드 순서대로 나열했다. 그 다음 로직에서는 i번째 문자가 다음 문자와 같다면, 카운트를 늘리고 그 다음 것과 같지 못하다면 프린트 하는 식으로 했는데, Blank 일 경우에는 따로 word[i]가 아닌 blank를 사용했고, 특수 문자일 경우 출력을 안하고 넘기는 식으로 작성했다.


+ **6번문제** 5번문제보다는 쉬운데, 소숫점 버리기와 입력에서 **_** 지우기가 까다롭다. 
>printf의 포멧팅을 사용해봐도, **precision()** 을 
사용해봐도 계속 반올림해서 cmath를 사용한 다음에, floor를 사용하기로 했다. 
>2번째 자리까지 남겨야하니, 100을 곱한 다음에 날리고, 100을 다시 나눠 줬다. ex| 1.2345 -> *= 100 ->  123.45 -> 123 -> floor() -> 1.23 **_** 이건 그냥 for문에서 찾아서 지웠다.


+ **7번문제** 쉬운문제. 
>그냥 I, X, V, L, C, D, M 이면, 더한다로 쉽게 풀었다.
>조건이 많아질 수록 어려운 문제.


+ **8번문제** 덜 쉬운문제. 
>7번문제에서 조금 발전한 문제다. 
>이것도 간단한데, 만약에 조건 **(I, X, C)** 문자일 경우 다음 문자가 조건 **[(V,X) (L, C), (D, M)]** 이면, 자신을 한 번 빼는 것으로 마무리


+ **9번문제** 학생을 랜덤한 방에 지정하는 프로그램, 한 방에 2명씩 들어갈 수 있고, 마지막에는 최종현황을 출력한다.

>랜덤으로 지정하는 것 자체는 어렵지 않다. persons라는 이차원 배열의 값이 2가 되면 그 방안에 2명이 이미 있는 것이니, 다시 돌리면 된다

생각보다 어려운 점은 출력인데, 마지막 출력 양식과, 모든 방의 현황을 보여주는 부분은 어려울만 하다. 

문제 길이 보고 겁먹지 말고, 내가 어떤부분을 구현해야 하는지, 이 코드는 어떤식으로 작동하는 것인지 차근히 생각해보면 자연스럽게

풀리는 문제다.


---


C++을 해보니, 확실히 C보다 재미있고 쉽다. C에서 사용 못했던 String 이라는 모듈을 사용하는 것도 그렇고 scanf 마냥 타입을 

지정해주지 않아도 알아서 지정되는게 정말 신세계다. 한 가지 아쉬운 점이라고 하면 **cout** 인데, Formatting을 직접 지정하는 것이

어려운 것이 마음에 안든다.